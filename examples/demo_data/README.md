# Demo Dataset

This folder contains a synthetic HL-style dataset for public evaluation and documentation.

- The files are not derived from a real account export.
- Values, dates, and asset names are fabricated.
- The dataset is safe to commit, share, and use in screenshots.

Run it with:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\run_demo.ps1
```
